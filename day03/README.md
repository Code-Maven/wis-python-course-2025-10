
## swap values
```python
    a = 2
    b = 4
    
    temp = b
    b = a
    a = temp

    a, b = b, a  # swap values without temp variable
```

```python
    for _ in range(len1 + 1):
        [0] * (len2 + 1)

    23
    2.3
    "ada"
    fruits = ["apple", "banana", "cherry"]
    zeros = [0, 0, 0]
    zeros = [0] * 3
```
